# Taskcal リポジトリ指示

## プロダクトと開発条件

Taskcalは、飲食店の突発欠勤に対して代替スタッフの調整を進めるサービスである。ハッカソンMVPは架空の1店舗、同時1案件、1職種、必要勤務枠1つを対象とする。2人で4日間に開発する。成果は制約を明示した試作であり、本番運用可能性の証明として扱わない。

Codexを主要な開発エージェントとする。主エージェントが計画、編集、統合、検証、最終報告を担当する。個別定義で明記しない限り、カスタムエージェントは読み取り専用のレビュー担当とする。

## 情報源の優先順位

現在の作業に必要な文書だけを読み、次の順で優先する。

1. ユーザーの現在の依頼と決定
2. `docs/adr/` にある採用済みADR
3. 現行RFC。特にRFC-009〜RFC-012
4. 未決の選択を記録した `docs/OPEN-QUESTIONS.md`
5. 履歴・根拠としての旧RFCと `docs/references/`

最初に参照する文書：

- ドメインと不変条件：`docs/rfc/RFC-009-domain-v04.md`
- CSV原本と正式採用：`docs/rfc/RFC-010-csv-authority.md`
- 打診、承諾、状態：`docs/rfc/RFC-011-outreach-and-state.md`
- 開発と受入：`docs/rfc/RFC-012-delivery-and-acceptance.md`
- 未決事項：`docs/OPEN-QUESTIONS.md`

レビュー上の提案、過去の値、未決事項を、黙って採用済みの決定へ変更しない。未決の値が実装を妨げ、ユーザー指定がない場合は、文書の初期推奨を変更可能なMVP既定値として使い、実装上の仮定であることを記録する。

## プロダクト境界

- MVPでは架空データと模擬メッセージ受信箱だけを扱う。
- 実モデル推論はサーバーからOrcaRouterを経由する。Gatewayのキーをブラウザやモバイルクライアントへ渡さない。
- モデルは返信解釈と限定された次行動の提案を行える。本人確認、認可、候補適格性、時間、承諾、上限、予算、状態遷移は決定的なコードで検査する。
- 認証済みの回答者、対象打診、日付、時刻、職種、意思が一意であり、全ての決定的検査を通るまで返信を承諾として扱わない。
- 欠勤理由を尋ねず、辞退理由の説明を求めない。過去の辞退を候補順位の減点に使わない。
- CSV出力を勤務表への反映完了と呼ばない。設定した正式採用、読戻し、通知の完了境界を満たす必要がある。
- 読戻しや通知に失敗しても確定済みの勤務を消さない。無条件に再実行したり旧版へ戻したりせず、結果不明として照合する。

## ドメインと状態の不変条件

`Schedule`、`AbsenceCase`、`Outreach`、`ScheduleUpdate`を別々の状態管理主体として扱う。通常勤務と代替勤務は同じ`ShiftAssignment`モデルで管理する。

全ての状態変更操作に、安定した操作IDとリクエストハッシュを持たせる。同じIDで異なる内容は拒否し、同じ内容は保存済み結果を返すか照合する。CSVの行順変更後も維持できる勤務IDを使い、行番号や表示名を識別子にしない。

正式採用の直前に、案件版、勤務表・原本版、最新の承諾版、未処理返信、停止状態、期限、スタッフ条件、月次入力の完全性を再検査する。選定した計画の全勤務を一括採用し、1案件で正式採用する計画は最大1つとする。

受信イベントは処理前に永続化する。返信順はモデル処理の完了時刻ではなく、永続化した受信順で決める。遅れて返ったモデル結果で新しい承諾を過去の状態へ戻さない。停止後は新規打診と正式採用を行わないが、確定済みの事実は保持する。

## 実装手順

1. 依頼に関係するRFC、ADR、未決事項ID、受入ケースを特定する。
2. 入力から永続化した結果まで確認できる、最小の縦断機能を実装する。ドメインロジックをUI、フレームワーク、Gatewayアダプターから分離する。
3. 外部作用の前に決定的な検査を置く。プロセス再起動後に復旧できる状態を保存する。
4. 変更した振る舞いを焦点の絞られたテストで検証する。該当する場合はA01〜A18をテスト名または追跡情報に使う。
5. 振る舞い、保証、境界、起動方法が変わった場合はADR、RFC、READMEを更新する。
6. 完了を報告する前に自己点検する。機械的検査を通し、変更した規則の対になる側、落ちる場所、境界値、文書の追随を確認する。アプリケーションコードを変更した場合は読み取り専用レビューエージェントへ委譲する。手順は`taskcal-self-review`にある。

新しい採用済み決定がない限り、Redis、Temporal、マイクロサービス、イベントソーシング、本番メッセージング、複数店舗、汎用管理画面を追加しない。

## 品質と証拠

- 決定的テストと実モデル評価を分ける。実モデルの成功を不変条件テストの代わりにしない。
- モデル、prompt/schema版、Routerの選択主体、トークン・費用と、それぞれが実測・推定・取得不能のどれかを記録する。
- タイムアウトや外部処理結果不明を、費用0または確定失敗として記録しない。
- 実行していない受入ケースを合格と記載しない。設計、fixture、目標、見積りを実績と区別する。

同種の指摘を繰り返し受けた分類は、人の注意に任せず機械的検査へ移す。検査を足したら、守るべき語を落として実際に失敗することを確かめる。
- 秘密情報、不要な個人情報、内部推論、不要なメッセージ原文をログやUIへ出さない。

アプリケーションコード追加後は、実際のformat、型検査、テストコマンドをルートREADMEへ記載する。コミット前に記載済みの該当コマンドを実行する。実行できない場合は成功を作らず、理由を明記する。

## 設計記録

アーキテクチャ、正本、セキュリティ、ドメインの意味、既存決定を変更する永続的な選択には新しいADRを使う。実装可能な契約、フロー、interface、受入条件はRFCへ記録する。古い決定を履歴として残し、本文を書き換えずに置換関係を明記する。

未決事項を確定する場合は、状態、関連ADR/RFC、受入期待値、`docs/CHANGELOG.md`を同じ変更で更新する。

## レビューエージェント

ドメイン規則、状態遷移、CSV正式採用、承諾、冪等性、並行処理の変更には`taskcal-domain-reviewer`を使う。MVP、セキュリティ、費用、UX、受入の最終確認には`taskcal-delivery-reviewer`を使う。独立して実行できる限定的なレビューだけを委譲し、主エージェントが結果を統合して編集を担当する。

レビューは重要度順に対応可能な指摘を先に示し、正確なファイルと行を記載する。指摘がない場合はその旨と、残るリスク・未実行の確認を示す。

## リポジトリ運用

- コード識別子と永続化するenum値は英語を使う。ユーザー向け応答、プロダクト文書、設計文書、エージェント指示は、ユーザー指定がない限り日本語を主要言語とする。
- 秘密情報、ローカル環境ファイル、生成した依存フォルダ、実在スタッフのデータをコミットしない。
- コミットを目的単位に保ち、無関係なユーザー変更を書き換えない。
- ユーザーの許可なくPush、merge、deploy、公開、外部への連絡を行わない。
