# 未決事項と決める時点

更新：2026-09-21（Q08を確定、Q11〜Q13を追加）。記録作成は完了している。以下は今後の実装に必要な選択であり、この資料作成を止めるための承認要求ではない。

## 業務・設計の選択

| ID | 判断 | 初期推奨 | 代替と影響 | 決める時点・担当 |
|---|---|---|---|---|
| Q01 | CSV出力の意味 | アプリ内の正式版に採用 | 出力のみなら自律反映完了とは呼ばない | 正式採用実装前・2人で選択、A設計 |
| Q02 | 必要人数の意味 | 全区間ちょうど1人 | 超過許容なら最大人数等と選定目的を定義 | 選定計算前・2人 |
| Q03 | 分断された空き時間 | MVP範囲外として拒否 | 区間集合を追加すればより多くの候補を扱える | 入力契約・B実装/A確認 |
| Q04 | 元勤務の部分欠勤 | 全時間欠勤だけ対象 | 部分対応なら残る勤務時間を集計し欠勤区間を保存 | fixture作成前・2人 |
| Q05 | 日跨ぎ | MVPは拒否 | 対応するなら営業日をまたいだ重複と月境界を検査 | 時間計算前・2人 |
| Q06 | 月次上限で数える時間 | 完了済みも予定区間で数える月次割当上限 | 実績＋予定には勤怠情報が必要。将来予定だけなら別の意味 | 月次計算前・2人 |
| Q07 | 業務完了と通知 | 正式確定・読戻し・必要通知受付まで | 確定と通知を別完了にするなら表示と指標を分ける。状態の対象別分離はADR-017で承認済み | 状態・指標の実装前・2人 |
| Q08 | 言語・DB・実稼働 | **確定（2026-09-21）**：TypeScript/Next.js/PostgreSQL | 下記「確定した選択」を参照 | Day 1・確定済み |
| Q09 | 自然文承諾の対応範囲 | 限定文型、曖昧・条件付きは確認 | 広い自由文対応は評価と解釈誤り対策の工数増 | schema・fixture固定前・A |
| Q10 | 件数・回数・費用等の上限 | 人数8、15分単位、最長4時間は旧案の初期候補 | 同時打診でcall・確認・通知数を再見積り。金額は未設定では呼ばない | Day 1、実課金前・2人 |
| Q11 | 照合が恒久的に不能な案件の出口 | RFC-011 §5の図に`ReconcileRequired → Attention`を追加 | 図には`Committed`か`Coordinating`しか出口が無く、どちらも持っていない事実の断定になる。§5本文は「ReconcileRequiredまたはAttentionとして人の対応を記録」と書いており本文と図が食い違う | A03の実装前・2人 |
| Q12 | 復旧しない`Attention`の終端 | `Attention → HandedOff`を追加 | 現在`Attention`から終端へ行けず、読戻し・通知が復旧しない案件が永久に非終端になる。A13/A18の「既確定の事実を保持して引き継ぐ」を状態で表現できない | A13の実装前・2人 |
| Q13 | 正式採用準備中の上限到達 | `Preparing → HandedOff`を追加するか、`Coordinating`を経由させる | 現在`Preparing`から`HandedOff`へ行けない。正式採用の準備中に予算・期限が尽きた場合の経路が無い | A18の実装前・2人 |

## 確定した選択

### Q08：言語・DB・実稼働（確定 2026-09-21）

選んだ値：TypeScript、Next.js App Router、PostgreSQL 18、番号付きSQL migration（pg）、
Vitest。ADR-003の候補構成をそのまま採用値へ昇格する。ADR-003は差し替えず、確定注記を
追記した。

理由：Aがフロント寄りフルスタック経験者であり、RFC-012 §3.1の所有パス表をそのまま
使える。両名の別の得意スタックを優先する根拠が出ていない。

同じ変更で反映した箇所：ADR-003（確定注記）、README（起動手順・確認コマンド）、
docs/CHANGELOG.md。受入ケースA01〜A18の期待値は、この確定によって変わらない。

実装上の仮定（採用済みのプロダクト判断ではない。変更可能）：

| 項目 | 値 | 理由 |
|---|---|---|
| Node.js | 20.19.5 | ADR-003の初期案は「Node 24系」。実機に入っていた20系がNext.js 16の要件（>=20.9.0）を満たすため、導入作業を増やさずこれをチーム共通版とした |
| TypeScript | 5.9系 | 最新は7系だが、4日間の期間中に新しいコンパイラを持ち込む危険を避ける |
| Vitest | 4.1系 | 5系はNode 22.12以上を要求し、上記のNode 20と両立しない |

Bの実機でNode版が異なる場合は、`.nvmrc`（20.19.5）に合わせる。

## 実装上の停止条件

### Q10：件数・回数・費用等の上限（未決のまま）

2026-09-21時点でOrcaRouterのAPIキー・接続先・単価が未取得のため、**実呼出しを行って
いない**。予算予約（`src/adapters/orca/budget.ts`）と使用量記録（`usage.ts`）の枠だけを
実装し、次の条件のいずれかで呼出しを開始しない（ADR-007）。

- 接続情報、`case_spend_limit`、`run_spend_limit`、1呼出しの見積りのいずれかが未設定：
  `createModelGateway`が`UnconfiguredModelGateway`を返す。
- 1呼出しの費用見積りが0以下または整数でない：見積り0を許すと金額上限の比較が常に成立し、
  予算が無効になる。

金額は**USDの整数micro単位**で扱う（RFC-004 §7の採用済み決定）。円換算は表示時に換算日時と
レートを添えて行い、内部では円を持たない。Q10で未決なのは**上限値**であって、通貨・表現では
ない。設定名もRFC-004 §7の`case_call_limit`／`case_spend_limit`／`run_spend_limit`に合わせる。

`case_call_limit`が未設定の場合は、ADR-007の初期値**10call／案件**をMVP既定値として適用する
（`DEFAULT_CASE_CALL_LIMIT`）。Q10が未決であることは「上限なし」を意味しない。
これは実装上の仮定であり、採用済みのプロダクト判断ではない。

予約は`request_id`で冪等にする。worker再起動やlease失効で同じ受信イベントを再処理しても、
二重に予約・課金しない。`request_id`は呼出し元が永続化し、adapter側で採番しない
（ADR-006）。課金不明は`UNKNOWN_CHARGE`として予約を残す（RFC-004 §7）。

### migrationの番号割当（実装上の運用）

RFC-012 §3.1は`src/adapters/db/migrations/`を「B作成・A承認」としている。これを維持した
うえで、`0001_worker_runtime.sql`のみworker基盤としてAが作成した。業務テーブルは0002以降を
Bが使う。所有区分の変更ではなく、番号の衝突を避けるための運用。

Q11〜Q13はRFC-011 §5の状態図に出口が無い経路で、2026-09-21の実装レビューで見つかった。
いずれもRFC-011の図の変更を伴うため、コード側で黙って遷移を追加していない
（`src/contracts/case-state.ts`は図に忠実なまま）。

対応ADR：[016](adr/ADR-016-csv-adoption.md)、[018](adr/ADR-018-scope-open-decisions.md)。決定時は選んだ値・日付・理由を追記し、RFC・fixtureの期待値を同時に更新する。

Q07は業務完了の時点だけが未決。案件・相手別対話・勤務表更新・メッセージ配送の状態を分離する方針は[ADR-017](adr/ADR-017-state-boundaries.md)で承認済み。

## 外部情報の確認

| 項目 | 現在の扱い |
|---|---|
| OrcaRouterの接続情報・モデル・単価・権限 | 公開資料と実アカウントは別。接続時に確認、取得不可はUNKNOWN |
| 主催の発表時間・提出形式・配点 | 未確認。3分実演は計画上の仮定 |
| 実店舗の勤務・連絡・雇用ルール | 現場検証なし。デモ条件を本番へ転用しない |
| ビジネスの価格・頻度・支払意思 | RFC-006の仮説を維持。売上や効果の実績ではない |
| 本番SaaS／LINEの具体接続先 | 未選定・未実装。現MVPの完成条件にしない |

チーム人数と経験構成は確認済みなので、再確認待ちには戻さない。
