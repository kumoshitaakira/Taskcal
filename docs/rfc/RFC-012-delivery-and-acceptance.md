# RFC-012：改訂版の開発・検証・移行計画

作成：2026-09-20／状態：実装計画案、作業未実施  
関連：[RFC-009](RFC-009-domain-v04.md)、[RFC-010](RFC-010-csv-authority.md)、[RFC-011](RFC-011-outreach-and-state.md)

## 1. 旧計画からの変更

RFC-005の2人・4日、経験者Aと初学者に近いBの分担方針を維持する。ただしCSV原本・正式版参照・同時対話が追加されたため、旧38人時を検証済み見積りとして流用しない。

1人1日6時間なら48人時という仮定は維持し、以下は40人時＋余裕8人時の新しい初期見積り。A22人時＋余裕2、B18人時＋余裕6で、Aへの集中が大きい。実際の得意言語・稼働・環境をDay 1に確認して再見積りする。納期の保証ではない。

## 2. 着手前に必要な選択

Q01正式CSVの採用、Q02超過配置、Q03分断時間、Q04部分欠勤、Q05日跨ぎ、Q06月次集計、Q07通知完了、Q09自然文の対応範囲を固定する。全てを広く実装することが完成条件ではない。範囲外を明示して拒否する案も有効。

言語・DB・CSV列・認証の実装詳細は後続。旧TypeScript／Next.js／PostgreSQLは候補構成であり、v0.4の記録が新たに製品選定を確定したとは扱わない。

## 3. 作業と担当

| ID | 作業 | A人時 | B人時 | 完了の証拠 |
|---|---|---:|---:|---|
| U01 | 未決仕様・共通型・起動契約 | 1.5 | 1.5 | 選んだ範囲と拒否条件がfixtureに反映 |
| U02 | CSV正規化・安定ID・月内fixture | 0.5 | 2.5 | 並べ替え・再読込でも同じ勤務を参照 |
| U03 | 区間・候補・月次・選定の計算 | 1 | 2 | 実行可能条件と集計をコードで検査 |
| U04 | CSV Gateway・結果照会・読戻し | 3 | 0.5 | 同じ操作の結果と更新後参照を取得 |
| U05 | 正式採用・一括保存・競合 | 3 | 0.5 | 次案件・再起動・二重採用に対応 |
| U06 | 店長・スタッフ・状態の画面 | 3.5 | 0.5 | 候補承諾／正式確定／通知待ちを区別 |
| U07 | 個別同時打診・返信・メッセージ管理 | 1 | 1.5 | 一人の確認中でも他の返信を処理 |
| U08 | Orca実接続・自然文契約・予算予約 | 3 | 0 | 原文・解釈・実使用モデル・費用状態を記録 |
| U09 | 永続処理・変更保留・停止・再開 | 2.5 | 1.5 | 未処理変更を無視せず、停止・復旧が機能 |
| U10 | 結合・障害・主要画面試験 | 1 | 4 | 下記の必須ケースを再現 |
| U11 | 品質・費用比較、結果記録 | 1 | 2 | 分母・失敗費用・未計測を表示 |
| U12 | 起動・初期化手順、実演・録画 | 1 | 1.5 | 同じ初期状態から再現 |
| 計 | | **22** | **18** | **40人時＋余裕8人時** |

Aが正式採用・AI・結合を担当する。Bはfixture・閉じた計算・試験を中心とし、Aが短い周期でレビューする。CSV接続と同時処理が予想以上に重ければ、機能追加を止めて入力範囲とUIを縮小する。

## 4. 日別ゲート

| 日 | 到達点 | 未達時の対応 |
|---|---|---|
| Day 1 | 仕様選択、CSV往復、実推論の最小接続 | CSV形式を固定。既存SaaS／LINEや汎用入力対応を増やさない |
| Day 2 | 返信・承諾・全体選定からCSVの正式採用まで | 文型とfixtureを限定。本人の意思・正式版・一括性は削らない |
| Day 3 | 競合、再起動、訂正、通知失敗、初回評価 | 新機能凍結。比較反復や装飾を削り、事故試験を優先 |
| Day 4 | 固定版評価、初期化、実演、資料 | ライブ障害なら事前実測・録画を再生と明記 |

CSV出力だけまで達成した場合は「変更案の出力」と報告し、自律的な勤務確定まで完成したとは説明しない。

## 5. 必須受入ケース

| ID | 入力・障害 | 期待する結果 |
|---|---|---|
| A01 | R1からR2を正式採用し次案件を開始 | R2の追加勤務を参照する |
| A02 | CSV保存後・正式採用前に停止 | 未採用CSVを勤務として数えない |
| A03 | 正式採用直後に応答喪失 | 操作結果を照合し、同じ勤務を再作成しない |
| A04 | 同じ旧版に対する二つの正式採用 | 一方だけ成立。別キーでも二重採用しない |
| A05 | 更新準備中に本人の訂正を受信 | 正式採用前に保留・失効を検知 |
| A06 | 入力CSVの行を並べ替えて再読込 | 勤務IDと案件の参照を維持 |
| A07 | 元CSVを誤って読戻す | 反映確認失敗として完了にしない |
| A08 | 全割当の一部の保存・適用で失敗 | 全件未採用。一部だけを正式勤務にしない |
| A09 | 月内入力不足／完了済みへの状態変更 | 未取得を0扱いせず、選んだ月次算式で残枠を守る |
| A10 | 別営業日の勤務変更・日跨ぎ | 検査した範囲の変更を検知。非対応なら入力を拒否 |
| A11 | A確認不能、B・Cで成立 | Aだけを理由に全体終了しない |
| A12 | 旧メッセージのAI結果が遅れて返る | 最新承諾を古い結果で復活させない |
| A13 | 確定通知失敗／終了後の追加返信 | 勤務確定を保持し、追加割当を作らない |
| A14 | 読取専用接続からファイル出力 | 出力のみと元原本反映済みを区別 |
| A15 | 宛先変更／他人の返信／接続別の同じイベントID | 本人・宛先の同一性を検査し、接続単位で重複排除 |
| A16 | B18〜20、C19〜22 | Q02で決めた超過配置ルールに従う |
| A17 | 空きが分断／元勤務の一部だけ欠勤 | Q03/Q04で決めた処理または範囲外拒否 |
| A18 | 店長停止、予算・回数・期限到達 | 新規打診・正式採用を止め、既確定の事実を保持 |

これは試験仕様であり、実行済み結果ではない。特にA16の期待値は未決方針を選んでから固定する。

## 6. AIと費用の評価

旧RFC-008の固定fixture、全高性能／全軽量／切替比較、失敗込み費用、正しい引き継ぎと自律完了の分離は維持する。旧20ケースをそのまま未修正で使わず、順次打診、ボタン同意、DB単独確定を前提にした期待値を更新する。

20ケース×3構成＝60実行、任意の追加12実行という規模は計画候補。回数達成より必須受入を優先する。モデルに依存しない事故試験は決定的な入力で行い、実モデル試験は別にする。費用予算未設定のまま実呼出しを始めない。

主な実演は「全員への個別打診→辞退・部分可否→成立する選定→正式版CSV→再読込→結果通知」。一つの訂正または成否不明の復旧も示す。更新CSVを見せるだけで、正式採用や次回参照の証拠に代えない。

## 7. 事業モデルへの影響

RFC-006の価格・顧客・ROIは未検証仮説のまま。CSVの正式採用がデモ内で成立しても、実店舗の既存シフト管理へ導入できる証明ではない。将来、毎回CSVの手動出力・取込みが必要なら、その時間を店長の負担と導入原価へ含める。

全員打診は順次打診より通知が増える可能性があるため、メッセージ費とスタッフ負担の仮定を再測定する。価格2,980円・10案件の仮説を再承認された料金と扱わない。

## 8. 記録と変更管理

実装開始時に、採用したQ番号、実装commit、fixture版、prompt版、Router設定版、結果を紐づける。文書の作成日を実装完了日としない。今回の資料追加によって元添付・レビュー・過去ADRの本文を改変しない。
